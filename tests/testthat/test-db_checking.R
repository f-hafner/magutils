

## testing
#indexes <- get_tbl_idx(conn, "PaperAuthorUnique")

# deal with missing index


# check: column names are in names of the table! (?)


# testing: remove other things? but what to keep? -- need to know the structure of this. is it always the same? ie does it have a standard?

